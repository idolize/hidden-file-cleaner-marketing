// @ts-check

/** @type {import('tailwindcss').Config} */
const config = {
  darkMode: 'media', // uses prefers-color-scheme
  theme: {
    extend: {
      fontFamily: {
        inter: ['inter'],
      }
    },
  },
}

export default config
